# Local development environment
NEXT_PUBLIC_API_SEARCH_URL=https://so.252035.xyz/api/search

# 可选: 添加注释说明如何自定义配置
# 如果需要指向不同的API端点，可以取消下面的注释并修改URL
# NEXT_PUBLIC_API_SEARCH_URL=http://localhost:8000/api/search

# 开发环境配置说明:
# 此URL指向开发用的搜索API端点
# 可根据实际需要修改为对应的开发环境地址